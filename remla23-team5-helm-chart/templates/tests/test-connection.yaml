apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "remla23-team5-helm-chart.fullname" . }}-test-connection"
  labels:
    {{- include "remla23-team5-helm-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "remla23-team5-helm-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
